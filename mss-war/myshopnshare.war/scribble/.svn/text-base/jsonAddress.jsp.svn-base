<%@ include file="/WEB-INF/jsp/includes.jsp"%>

<json:object>
    <json:object name="billing">
   	  <json:property name="id" value="${billing.id}"/>
      <json:property name="street1" value="${billing.street1}"/>
      <json:property name="street2" value="${billing.street2}"/>
      <json:property name="unitNumber" value="${billing.unitNumber}"/>
      <json:property name="city" value="${billing.city}"/>
      <json:property name="stateOrProvince" value="${billing.stateOrProvince}"/>
      <json:property name="country" value="${billing.country}"/>
      <json:property name="postalCode" value="${billing.postalCode}"/>
      <json:property name="primaryAddress" value="${billing.primaryAddress}"/>
      <json:property name="addressType" value="${billing.type}"/>
    </json:object>
    <json:object name="shipping">
   	  <json:property name="id" value="${shipping.id}"/>
      <json:property name="street1" value="${shipping.street1}"/>
      <json:property name="street2" value="${shipping.street2}"/>
      <json:property name="unitNumber" value="${shipping.unitNumber}"/>
      <json:property name="city" value="${shipping.city}"/>
      <json:property name="stateOrProvince" value="${shipping.stateOrProvince}"/>
      <json:property name="country" value="${shipping.country}"/>
      <json:property name="postalCode" value="${shipping.postalCode}"/>
      <json:property name="primaryAddress" value="${shipping.primaryAddress}"/>
      <json:property name="addressType" value="${shipping.type}"/>
    </json:object>    
</json:object>